{
  "name": "@public-official-guide/schema",
  "version": "0.0.1",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:reset": "prisma migrate reset",
    "lint": "echo 'No linter configured for schema'",
    "test": "echo 'Schema has no tests'",
    "dev": "echo 'Schema is a library'"
  },
  "dependencies": {
    "@prisma/client": "^6.0.0"
  },
  "devDependencies": {
    "prisma": "^6.0.0",
    "typescript": "^5.4.0"
  }
}
